# 0.10.0：原生界面与交互

本阶段已完成并安装：连接、节点和设置整理为三个主入口，保留现有节点数据与连接控制。应用名称为Harmony VPN，bundle仍为`com.example.harmonyvpnlab`，版本0.10.0/code29。没有修改Xray/Hev或VPN服务代码，没有Git提交或推送。测试结束时已恢复跟随系统外观与正常字号，VPN正常断开。

## 界面与导航

- 首页集中呈现连接状态、当前节点、连接按钮和流量。没有本进程接受过的流量快照时显示“—”，不把默认0当作上次流量。开发验证移到设置中的开发工具，连接检查在连接期间显示。
- 主导航保留Home根页，Home→Nodes/Settings使用push，Nodes与Settings互切使用replace，返回连接页使用back。保留根页的授权观察器和原来的连接命令所有权。导航有并发保护；同步back另保留250ms防止旧页连续点击重复退栈。
- 节点采用紧凑列表，添加入口使用原生菜单，改名/删除收进更多菜单。检测结果独占一行，时间采用短格式；长名称最多两行，大字体的耗时数字完整显示。取消检测固定在列表下方。连接期间可浏览，修改和检测仍受原业务保护。
- 设置提供跟随系统/浅色/深色、诊断、关于、隐私说明及开发工具。外观仅改变本应用，采用严格枚举、小文件、原子写入；保存失败回滚当前外观。
- 导入、订阅、诊断统一主题资源；服务器地址修改和保存UUID回执放入可展开区域。没有新增或改写导入、订阅、测量、删除的核心业务逻辑。
- AppScope显式引用configuration profile，`fontSizeScale`为`followSystem`、`fontSizeMaxScale`为字符串`2`。正文行高使用fp，按钮用最小尺寸，短内容滚动区域从顶部排列。

## 编译与离线回归

DevEco Studio26 SDK/API26整包编译、打包与调试签名成功。HAP 40,488,249字节，SHA256：`8dd9d1f387d2a65a34b53de6d780e1a973273c34770ae1735a50de30b5d8edbe`，按哈希归档至`build/artifacts/versioned/0.10.0/8DD9D1F387D2-entry-default-signed.hap`。最终总记录见`build/device-tests/phase10-verification.json`，最新指针`build/verification.json`已更新，阶段九原记录另行保留。

| 测试 | 结果 | 范围 |
| --- | --- | --- |
| 连接会话 | 57/57 | 保留原54项，新增开发视图生命周期3项 |
| 节点管理UI | 79/79 | 保留原保存、取消、迟到结果、配置指纹等断言 |
| 主导航 | 15/15 | 实际导航模块，push/replace/back、保留Home、防重复及失败恢复 |
| 外观 | 15/15 | 实际保存/加载/设置方法与合成SDK，非法数据、写入故障和回滚 |

合计166项全部通过。恢复收尾时复核了现有测试结果，连接会话、节点UI和导航记录中的源码SHA与当前文件吻合；外观日志不包含源码哈希。本次恢复没有重复运行已经完成的测试。

测试工具也适配了新界面：`test-product-ui-device.cjs`只允许明确导航、外观切换和截图；导航过渡最多5秒只读等待，连续两次确认后才继续，锁屏/权限弹框/外部应用立即停止。`test-node-latency-device.cjs`改从添加按钮判断编辑能力、从固定子标签判断选中状态，不读取节点名称或父按钮私密文本。`test-connection.ps1`识别固定homePage，开发测试模式自动进入开发视图，非连接状态不发起HTTPS检查。

## 真机视觉与交互

设备沿用Pura80Ultra/HarmonyOS7。观察过浅色首页、深色设置/节点、导入、订阅页面；在真实截图中修复了主题图标导致的按钮高度不齐、圆角被覆盖、短列表居中留白、选中标签高度和大字体耗时被挤短的问题。截图只保存在工作区`.private/phase10-screens`，未公开发布。

大字体通过`uiFontScale=2`参数调用应用级setFontSizeScale，不更改系统字体、不写入偏好。源码以`applicationInfo.debug`门控，仅在调试应用中应用该参数；本轮没有构建或验收release包。手机日志记录`UI_FONT_SCALE_TEST scale=2`，真实截图核对首页、设置和节点；最终节点截图显示两条HTTPS耗时完整。确认VPN已结束后重新启动UI、不带QA参数，恢复系统字体。

实际操作的安全投影保存在`build/product-ui-tests/phase10-actions.jsonl`，字号操作在`phase10-font2-actions.jsonl`。它们只保存固定控件ID、启用/选中状态及截图路径；原始AX树不保存到电脑，只在设备临时文件与工具内存中短暂处理，设备临时文件读取后删除。本轮实际打开导入和订阅表单并返回，没有重新扫描二维码或访问真实订阅服务。关于、隐私说明、开发工具与返回路径均有完成记录。

## 连接与节点回归

较早0.10候选（HAP `cced5db213042bca589ab97580a3d51d1d466e90fa16a767f628f586bf2297f6`）的受控全局连接run`1788875979667`，UI21649/VPN23430。经过节点/设置互切、浅色/深色/跟随系统切换、开发工具打开与返回，三次DNS+HTTPS成功，恢复数0；停止时保护30/30、失败0、超时0、active0，TUN关闭、网卡销毁、cleanup与扩展退出均确认。后续修改仅涉及布局和显示格式。早期还有一条非受控会话，历史不完整，不将其用作验收证据。

最终8dd9包：第一节点run`1788877202717`，2461ms；取消run`1788877325798`，明确cancelled/stopped；取消后再测run`1788877361766`，2385ms。三次均cleanup=true，节点数量、两条ID与原选中项保持，测试按钮恢复可用。详见`build/node-latency-tests/2026-09-08T14-*`的TestFirst/TestCancel记录。第二节点已有检测记录保持，但本轮最终包未重复它的联网检测。

最终8dd9安装包另有独立全局连接验收：run`1788877558402`，UI37729/VPN43338，22:25:58进入Wi-Fi active。首次DNS+HTTPS于22:26:39通过；切换深色模式并返回首页后，22:29:00第二次通过。两次按时间去重，不将后一个日志文件包含的历史行重复计数，亦不与早期候选的三次检查混算。

随后恢复跟随系统外观并停止：22:29:31保护26请求/26成功/0失败/0超时/active0；Hev正常退出、Xray停止、TUN关闭、网卡销毁、cleanup=true及扩展销毁均确认。最终页面显示未连接，原两条节点和选中项保留。

| 最终包证据 | 文件 |
| --- | --- |
| 全局启动 | `build/connection-tests/20260908-102556-Start.log` |
| 首次联网 | `build/connection-tests/20260908-102600-Check.log` |
| 深色模式返回后联网 | `build/connection-tests/20260908-102856-Check.log` |
| 停止和清理 | `build/connection-tests/20260908-102929-Stop.log` |
| 未连接界面 | `build/connection-tests/20260908-102929-Stop-layout.json` |

用户暂停后恢复，本次仅核对和整理证据：安装版本仍为0.10.0/code29调试包，USB在线，UI进程37729仍在，未查到VPN扩展进程。本次未确认应用处于前台，也未为此启动应用或改变手机设置。安全读回结果保存在`build/product-ui-tests/phase10-resume-audit.json`。

## 范围

这是界面和日常交互版本，使用DevEco调试签名；没有提交商店。关于页是主要开源组件的许可证摘要，隐私说明依据当前行为编写；正式分发仍需完整第三方许可、源代码提供安排和正式隐私/发布材料。本轮未重跑阶段九的30分钟观测，不把它记为0.10新的长时或续航证据。系统字体跟随已配置并做应用级2倍字号检查，未宣称覆盖所有输入法、横屏、机型或无障碍读屏场景。
