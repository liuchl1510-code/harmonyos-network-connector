# 0.16.0 恢复复验与说明修正

2026-09-12。本次只修改 About、Privacy 两页，版本保持 0.16.0/code37。DNS、测速、分流、C++ 和原生核心源码未变；阶段十九的远端解析等实验没有进入正式包。

- About 使用本应用的系统包信息读取版本，进入页面时更新；预览包保留“界面预览”标记，读取失败显示“版本信息暂不可用”。接口最低 API 10，可用于项目的最低 API 24。
- Privacy 区分连接检查与节点检测，明确节点检测最多顺序发起两次 HTTPS 请求、分别计时，仅确认连接复用时显示复用延迟；记录说明补充第二次状态、耗时和复用信息。保留不保存响应正文或出口 IP 的说明。

ARM64 和 x86_64 预览包各通过 13 项构建、源一致性、ABI、签名与变体核验。106 个源输入在构建前后不变，相对阶段十八冻结记录只有上述两页差异。

| 产物 | 大小（字节） | SHA256 |
|---|---:|---|
| ARM64 0.16.0 | 41,089,482 | `3327f0b459dd8d1d838951caf9d94dece75a3c76697bea9bfe92ed0c12d56148` |
| x86_64 0.16.0-ui-preview | 3,249,443 | `a4d3eac0f12a498982e0c4ec0ba6fe0192d97a1758374664976c442c00ebc1de` |

手机已安装此 ARM64 包。界面读回确认“版本 0.16.0”与安装包信息一致，滚动确认双请求及记录保存说明实际可见。预览包本轮只完成构建核验，没有新增模拟器界面验收。

恢复后的旧版联网复验一度出现 8 秒超时；该失败发生在安装两页修正包之前。用户随后重新导入节点并触发检测，助手读回核对新节点指纹与当前会话结果一致：首次 HTTPS **1767 ms**、第二次 **178 ms**，连接复用确认，正常 DoH 查询 1 次、FakeDNS 回答 0 次，代理双向流量为正，且清理确认 `cleanupConfirmed=true`。

这证明重新导入后的节点在正常 DoH 路径上完成本轮检测与清理，不代表已逐字段确定旧节点超时原因。当前节点库为用户重新导入后的 1 条记录，完整保留；网络策略和外观未改变，没有用旧备份覆盖新节点。

本地证据位于 `build/phase20-arm64/candidate1-20260912T121835Z/`、`build/phase20-preview/candidate1-20260912T121835Z/` 和 `build/phase20-phone/`。私人配置、原始设备数据和日志仅留在 `.private`，不随源码发布。
