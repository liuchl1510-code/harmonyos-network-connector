# 变更日志

## 0.26.0 — 2026-09-26

- 为同一 DNS／核心实例内的普通远端 HTTPS DoH 解析器复用 HTTP2 transport，条件为完整 URL 与有效 inbound 标签相同。缓存、ECS、超时、查询策略、规则顺序和 `finalQuery` 保持独立；不同标签、URL、实例及 local／h2c 不共池。
- 停止时取消所属请求并关闭已跟踪连接；在 TLS 握手前跟踪原始连接，关闭迟到的 dispatch／dial 结果。单请求取消不关闭其他请求的共享池，成功连接保留自己的 dispatch 生命周期。此关闭路径不承诺等待所有后台 goroutine 退出。
- 增加固定上游四文件补丁及源码哈希，标准 native 构建自动执行真实本地 HTTP2／生命周期验证；公开策略验证器新增 `-DoHTransportPool`。应用补丁固定 LF 输出，避免本机 Git 换行设置影响完整性核对。
- 修改 Go DNS 层，未修改 C++ 桥接或 Hev 源码；九个 C ABI、原有 socket 保护与统计接口保留。

版本 code48。21 个本地叶子场景、三轮 race 共 63 次叶子执行无数据竞争，42 个策略场景及 7 个路由断言通过，负向对照按预期失败。最终 ARM64 库与签名 HAP 均核验，手机覆盖升级保留 7 份用户文件。12 个 DNS／独立 HTTPS 会话、4 个连接复用会话的 48 项测量全部通过；另一次日常连接 DNS／HTTPS 检查通过，17 个会话均清理。分流第二类别首次 DNS 中位数为 221／225.5 毫秒，没有新增 DoH dispatch；冷启动 HTTPS 未显示稳定改善，长尾样本保留。各候选、TLS 与源码证据边界见[阶段三十三记录](HarmonyVpnLab/docs/phase33-doh-pool.md)。

## 0.25.1 — 2026-09-26

- 增加受控 Cloudflare HTTPS keep-alive 诊断，分别记录首次和第二次 GET；每次请求创建独立 `HttpRequest`，保留到整轮结束后统一释放。API 26 才显式请求连接复用，实际是否复用以 SDK 返回为准。
- 取消、超时、会话变化和释放异常阻止成功收据；保留原有强制关闭连接的测试目标。SDK 时序按累计里程碑解释，不能相加或称为纯节点 RTT。
- 没有修改 DNS 分流策略、C++、Go 或上游转发核心；诊断入口仅用于完整核心调试包。

版本 code47，35 项诊断方法测试及 30 项汇总器断言通过，ARM64 C1 签名包 13 项核验通过、132 个源输入一致。在一台 API26／ARM64 手机上完成 0.25.0 的 12 次基线会话和 0.25.1 的 4 次连接复用会话，共 28 份收据、48 项测量。全局与 DNS 分流模式的复用请求中位数分别为 190／193 毫秒，各有两次 SDK 实际复用确认。16 次会话均清理，最后 VPN 断开，7 份用户文件按字节保留；没有切换手机网络或操作鸿蒙电脑。首次开销、DNS 连接类别差异及样本边界见[阶段三十二记录](HarmonyVpnLab/docs/phase32-first-access.md)，本版本不宣称转发核心性能已优化。

## 0.25.0 — 2026-09-26

- 白名单 DNS 分流连接的“检查连接”分别显示直连 DNS、经节点 DNS 与 HTTPS 结果；直连查询失败后继续检查代理路径，保留两者各自分类。检查使用服务冻结的有效 DNS 模式，不根据新偏好猜测当前运行路径。
- DNS helper 校验响应问题中的固定域名，区分查询超时、格式异常与查询失败；绑定、发送与等待回答共用每条查询 6 秒预算，清理另有 1 秒预算。取消、迟到 bind 和未确认 close 不会报为成功；网络或会话变化后旧检查结果失效。
- 新增只在完整核心调试包可入场的受控 DNS 验证：固定正常、HTTP 404、极短超时三类；只接管本应用，使用独立内存策略，不保存网络偏好。故障验证最多 90 秒，正常恢复验证最多 5 分钟，并沿用正常停止与清理流程。
- 同步固定失败阶段与建议，保留旧诊断原因索引及单服务写入历史的约定。没有修改 C++、Go 适配或上游核心源码。

版本 code46，ARM64 C1 调试构建 13 项核验通过、132 个源输入一致。API26／ARM64 手机从 0.22.0 覆盖升级后，7 份用户文件按字节一致；正常分流检查、真实直连 DoH HTTP 404 和核心极短超时故障已核对，故障期间代理 DNS／HTTPS 仍可通过，未观察到百度查询改走代理 DNS。极短超时会话在未发停止命令的情况下约 90 秒自动结束，清理、进程退出及 7 份用户文件保留通过。同会话断网自动恢复、离线等待时主动停止、恢复后约一分钟不重连及日常模式复验已核对，最终 VPN 断开、7 份用户文件按字节保留；测试方法、离线用例数与边界见[阶段三十一记录](HarmonyVpnLab/docs/phase31-dns-recovery.md)。

## 0.24.0 — 2026-09-23

- 网络设置新增默认关闭的“白名单 DNS 分流”；仅在白名单模式生效，Google 域名类别优先经节点，内置直连域名类别使用直连 DoH，其余域名仍经节点。
- 保留原代理 DoH 设置，新增可编辑的直连 DoH，默认阿里公共 DNS `https://223.5.5.5/dns-query`。初期直连地址仅接受 HTTPS IPv4 地址及路径；查询失败不自动改走另一条 DNS 路径。
- 网络策略升级 schema 3，旧 schema 1／2 仅内存迁移，显式保存才写回。切换为全部代理或自定义规则时保留 DNS 分流偏好，但不启用；非默认直连地址保留可见，非法地址可就地修正。
- 同步首页与隐私说明，明确直连提供方可见查询域名及物理网络出口、公共 DoH 不保证解析局域网私有域名；节点检测仍使用独立默认配置。未修改 C++、Go 或上游转发核心。

版本 code45。表单套件 140 项、DNS 策略 29 项、固定 Xray 实际 DNS／路由器 42 个场景通过；ARM64 C2 调试构建已核验。一台 API26／ARM64 鸿蒙电脑完成 18 次连接对照、30 项网络测量、查询路径核对、手动重连、宽窄窗口及双倍字号检查；测试后恢复原网络策略并正常断开。百度目标改善明显，Cloudflare 未改善；真机直连 DoH 返回 404 的故障注入未完成，证据边界见[阶段三十记录](HarmonyVpnLab/docs/phase30-split-dns.md)。

## 0.23.0 — 2026-09-23

- 网络设置新增“白名单（绕过大陆）”，采用 v2rayN 7.24.9 内置规则的原顺序和固定 GeoData 分类；保留 Google 代理及 UDP 443 阻断等例外，未命中走代理。
- 打包可复现的必要分类，启动前校验完整性并安全准备缓存；保留原 DNS、IPv6 和应用范围行为。未修改 C++ 或 Go 核心。
- 设置按钮适配大字号与窄窗，保留原自定义规则；补充数据来源、许可及隐私说明。

鸿蒙电脑实测全局 → 白名单 → 全局：百度请求依次走代理、直连、代理，三次 DNS／HTTPS 检查及断开清理通过。实际核心离线路由 38 项断言通过。完整验证范围见[阶段二十八记录](HarmonyVpnLab/docs/phase28-whitelist-routing.md)。

同版本后续完成约 30 分钟后台观察、系统睡眠唤醒后同会话联网、断网自动恢复，以及主动断开后恢复网络不重连的观察。本轮未修改应用代码；范围及资源采样口径见[阶段二十九记录](HarmonyVpnLab/docs/phase29-pc-stability.md)。

## 0.22.0 — 2026-09-19

2026-09-22 补充实测：一台 API26／ARM64 真实鸿蒙电脑通过完整核心安装、原生运行时检查、两次 VPN 连接与清理、最小化后返回，以及诊断摘要复制和 TXT 保存。宽窄窗口及两倍字号以实际控件几何检查，睡眠和长期后台未覆盖；应用逻辑与原生核心未因此次签名和测试改变。见[阶段二十七记录](HarmonyVpnLab/docs/phase27-real-pc.md)。

- 连接诊断新增摘要预览，按白名单整理已保存的事件、网络变化、失败分类与清理回执，内部会话使用本次摘要中的临时编号。
- 支持复制到本机剪贴板和保存 TXT；两者使用同一份预览，保存后核对 UTF-8 字节、长度与 EOF，分别提示取消、写入失败和核对失败。
- 区分未找到记录、有效空记录、格式异常和读取失败；保留旧日志读写行为，不因查看摘要重写原数据。明确记录快照不等于实时联网状态或已确定的故障根因。
- 保留文件选择器返回后的预览，阻止重复操作和迟到回调；同步隐私说明。修复实际设备上初始空文本编码导致摘要生成失败的问题。

版本 code43，原生核心未改。408 个执行案例与 17 个静态检查通过；手机／电脑模拟器检查正常、损坏与额外字段记录及复制、取消、宽窄布局。真机完成预览、复制反馈和 TXT 保存后的应用字节核对，12 个原文件保持，未新建 VPN 会话。各候选及验证边界见[阶段二十六记录](HarmonyVpnLab/docs/phase26-diagnostic-summary.md)。

## 0.21.0 — 2026-09-19

- 将连接、节点、设置改为悬浮玻璃胶囊，右侧提供独立圆形快捷启停；宽窗口保留侧导航。
- 页面内容滚动经过导航后方，移除全宽实色底栏；支持 API 26 系统沉浸材质，旧版本或能力不可用时降级为半透明模糊。
- 修正页面与内部页签栏的绘制裁切，让内容与胶囊阴影延伸到系统安全区；顶部增加随窗口避让高度变化的柔和过渡，保护状态栏文字。
- 根据导航实际高度保留滚动尾部空间，支持两倍字号与末项操作。快捷启停复用首页原流程，重新验证会话、状态和请求时效。

版本 code42，C++ 与原生核心未改。415 个执行用例和 36 项静态检查通过；手机与电脑模拟器检查深浅色、宽窄窗口和两倍字号。真机确认底部阴影连续，并通过新按钮连接、DNS/HTTPS 检查和断开清理，最后保留数据并恢复浅色。玻璃通透程度按用户偏好保留。各候选证据分别记录，详见[阶段二十五记录](HarmonyVpnLab/docs/phase25-floating-navigation.md)。

## 0.20.0 — 2026-09-15

- 网络设置新增全部应用、绕过所选应用、仅代理所选应用三种范围；首页展示应用范围及原有流量规则摘要。
- 提供常见应用包名预设，支持搜索、勾选及按包名添加；不自动读取安装状态，用户按本机实际安装情况选择。客户端自身固定纳入以进行连接检查。
- 网络策略升级 schema 2，旧设置只在内存迁移，明确保存才写回。空选择、无效包名、尚未加入列表的输入和返回确认期间不会提交设置。
- VPN 创建前验证并固定策略快照，应用范围与核心规则一致，重连继续使用同份策略；临时节点检测保持只接管本应用。
- 修复手工添加后程序清空输入触发回调、擦掉成功提示的问题；保留未添加输入的返回确认和保存拦截。

版本 code41，C++ 与原生核心未改。398 项合成／方法测试和 14 项 AST 检查通过，两包分别通过 13/15 项核验；手机上观察到浏览器纳入时与 VPN 出口一致、绕过时使用独立 IPv6 出口，随后恢复原策略语义并正常清理。最终提示修正版完成 PC 定向复验及手机覆盖升级，两候选证据分别保留。完整结果见[阶段二十四记录](HarmonyVpnLab/docs/phase24-app-routing.md)。

## 0.19.0 — 2026-09-14

- 节点导入与编辑按字段展示配置问题和建议；批量导入保留部分成功确认，并清除后续全失败结果之前的待保存预览。
- 首页增加“检查配置”，连接前自动检查并在异步授权等待后重新核对节点；本地检查通过明确不代表联网成功。
- 首页与诊断页显示实际失败阶段，首次失败、清理失败和暂时性问题分别处理；不依据原始错误文字猜测根因。
- 服务历史增加固定阶段和原因代码，保留原有容量与单写入方；更新隐私说明及多端建议措辞。
- 切换到物理网络 DNS 检查时清除上一轮 HTTPS 失败卡片，避免新结果与旧提示混在一起。

版本 code40，没有修改 C++、原生核心、DNS、分流或测速请求。15 套件共 898 个命名合成用例通过，另有 11 项静态检查；ARM64 和 x86_64 预览构建分别通过 13/15 项核验。手机与电脑模拟器检查错误输入、草稿保护及宽窄窗口/两倍字号；API 26 手机保留当前配置升级，通过本地检查、正常连接、DNS/HTTPS 与最终清理。异常故障提示由合成方法测试和模拟器覆盖，没有在手机注入坏节点。完整结果见[阶段二十三记录](HarmonyVpnLab/docs/phase23-configuration-and-failures.md)。

## 0.18.0 — 2026-09-13

- 首页、节点操作限制与开发验证入口使用统一生命周期判断；服务明确退出后可重新连接，旧 UI 标识或过期心跳不能单独解除限制。
- 诊断页区分真实清理回执与应用观察，新增最多 50 条独立退出观察，保留原始服务状态及日志。
- 恢复操作、通知等待后的连接及旧测速取消重新核对会话；异常退出会停止旧测速队列，不自动重启后续节点。
- 过期、未入场的旧授权请求可显式撤销；开发验证也遵守旧服务门禁。同步隐私说明与异常恢复回归。

版本 code39，C++ 与原生转发核心没有修改；本轮验证状态见[阶段二十二记录](HarmonyVpnLab/docs/phase22-session-recovery.md)。

551 个命名合成用例及 180 个操作限制状态组合通过，两个构建各通过 13/15 项核验。手机与电脑模拟器检查异常状态及普通/两倍字号；API 26 手机完成覆盖升级、整应用强制结束后的恢复、观察去重、再次联网及最终正常清理。单独终止服务的外部命令受手机权限/可见性限制，未计为真机通过。

## 0.17.0 — 2026-09-13

- 首页新增实时代理上传、下载速率，保留累计流量与时长；按实际采样间隔计算，在重连、后台返回及无效样本后重建基线。
- 节点页支持原始顺序、首次 HTTPS 耗时、已确认复用延迟三种排序。不可比较的结果排后，同值保持原序，不改变当前节点。
- 更新相应离线测试和设备验证工具。版本 code38，C++、原生核心、DNS 与网络策略未修改；验证状态见[阶段二十一记录](HarmonyVpnLab/docs/phase21-rates-and-sorting.md)。
- 修复验证工具把其他 Offline 模拟器误认为指定 USB 手机离线的问题，继续严格核对被选中的设备。

12 套件合计 536 个合成用例／场景通过；另补设备选择回归。ARM64 完整核心包通过 13 项核验，x86_64 预览包通过 15 项核验。手机、平板、PC 模拟器检查普通及两倍字号，PC 另检查宽窄窗口。API 26 手机保留当前节点覆盖升级，实测速率、HTTPS、重连及后台返回，最终收到本轮真实清理完成回执。统计仅包含代理出站，不代表带宽测试或平板／PC 联网验收。

## 0.16.0 — 2026-09-11

- 节点检测分别显示首次 HTTPS 与第二次请求耗时；仅系统确认连接已复用时标为“复用延迟”，否则注明新建连接或复用未确认。
- 第二次请求失败单独提示并保留首次成功结果；两次均独立计时，不以最小值替代首次耗时。每请求 8 秒、合计 16 秒，原临时 VPN 30 秒上限与取消保护保留。
- 结果存储升级为 schema 2，兼容读取旧 schema 1；旧记录明确标注复用尚未检测。排序文字改为“按首次耗时排序”，避免混排不同口径。
- 增补协议兼容、取消、超时、第二次失败、迟到结果、服务保存与界面标签检查，并更新设备验证工具的固定标签解析。
- 关于页从系统读取本应用实际版本，保留预览标记；读取失败时显示不可用，避免硬编码版本过期。
- 隐私说明同步为最多两次节点 HTTPS 请求及第二次结果、耗时、复用信息的保存范围。

版本 code37，C++ 与原生转发核心源码未修改。此次是测量与显示语义的完善，不表示首次连接或转发性能获得同等幅度提升。构建和设备结果见[阶段十八记录](HarmonyVpnLab/docs/phase18-latency-metrics.md)。

ARM64 完整核心和 x86_64 预览调试签名构建各 13 项核验通过，九组离线合成检查共 429 项通过。API 26 手机完成保留配置与旧记录的覆盖升级，两条节点分别实测出首次 2533/2574 ms、复用 256/220 ms；取消后重测也通过，最终确认 VPN 已清理、首页未连接。本轮没有新增 API 24 平板或 PC 联网验收。

2026-09-12 的两页收尾修正保持 0.16.0/code37，两个新包各 13 项核验通过；手机实际确认版本号与双请求隐私说明。联网复验起初超时，用户重新导入后，读回确认正常 DoH、HTTPS 与清理通过。新导入的数据保留，详见[收尾记录](HarmonyVpnLab/docs/phase20-restoration-and-copy.md)。

## 0.15.0 — 2026-09-11

- 新增节点收藏和来源筛选，可与搜索及 HTTPS 耗时排序组合；查看详情与选用节点分开操作。
- 宽窗口新增独立详情栏，窄窗口可展开行内详情。修复同一节点收藏、改名后列表或详情保留旧内容的问题；改善两倍字号下的侧栏品牌和收藏按钮排版。
- 导入并读回确认后可直接查看本次导入的节点，包含已有重复项；新请求有独立标识，清除筛选后返回页面不会被旧请求覆盖。
- 节点库与备份升级为 v2，收藏随备份保留；旧 v1 内容经严格校验后在内存迁移，下一次实际修改再原子写入。新备份需 0.15 或更新版本读取。
- 列表读取失败时保留错误与待处理导入请求；选择、收藏、改名或删除已提交但读回失败时，明确提示列表尚未确认。

版本 code36，完整核心 ARM64 与 x86_64 界面预览调试签名包均已核验，min API24/target26。最终候选通过 25 套件离线回归；各候选的真实界面操作、文件恢复和运行观察分开记录。原生转发核心未修改；验收范围见[阶段十六记录](HarmonyVpnLab/docs/phase16-node-collection.md)。

已在 API 26、ARM64 手机完成 0.11.1→0.15.0 覆盖升级，核对原两条节点、当前选择、原收藏状态和设置保留。一次约 202 秒的 Wi-Fi 连接通过代理 DNS 与域名 HTTPS 检查；测试后正常断开，清理确认且服务进程已退出。平板/PC 联网、全部协议和长期续航未由本轮验收。

## 诊断与验证工具更新 — 2026-09-10

应用仍为 0.14.0/code35，常规应用及原生核心源码没有变化。

- 长测新增启动清单、逐样本刷盘和原子 checkpoint，明确区分已观察样本、用户停止、失败与正常完成；新增只读记录检查器。
- 单独记录布局读取耗时，仅将该命令等待上限调整为 30 秒；没有自动重试，失败记录继续保留。
- 修复采集器在末尾不足 4 秒时提前结束的问题；独立检查器拒绝时长不足的完成结论，图表明确标注未满请求时长的历史记录。
- 新增隔离的堆诊断模板和结构分析工具。四份真实快照显示所检查的页面、状态与订阅实例没有随五轮导航递增，部分大数组在静置后收缩；这不代表排除所有泄漏。
- 原始堆快照加入忽略规则，公开材料仅保留工具、合成测试及汇总。

正常 PC 预览包保留 59 分 57.29 秒、190 个样本和 183 次导航的同进程观察；该轮没有达到完整 60 分钟，不计为完整通过。六组离线检查共 213 项通过，堆诊断与普通预览包分别记录，详情见[阶段十五记录](HarmonyVpnLab/docs/phase15-memory-observation.md)。

## 0.14.0 — 2026-09-10

- 分流与 DNS、节点导入及订阅增加未保存返回确认、重复操作与迟到回调隔离；失败保留输入，提交后读回异常与写入失败分别反馈。网络设置反馈及保存区固定在表单底部。
- 修复分流模式按钮在 360 vp 窗口、2 倍字号下横向溢出的问题，改为按可用宽度换行；已在 PC 和平板模拟器观察布局。
- 节点编辑兼容 ALPN 字符串和旧 WebSocket Host 头；修复 PC 模拟器真实操作中发现的取消返回后输入回退问题，保留草稿及模态期间业务限制。
- 节点备份不再因系统文件选择器引起的页面隐藏而误取消；明确返回、销毁及恢复前校验继续生效。
- 节点列表减少排序和历史匹配中的重复查找，复用未变配置的指纹，并避免首次显示时重复读取。
- 模拟器辅助脚本增加候选包身份绑定、时间/停止边界、真实采样跨度及可重试的合成夹具恢复；新增空闲及重复界面检查观察模式。

ARM64 完整核心与 x86_64 预览调试签名包已核验，均为 code35、min API24/target26；本轮未修改原生转发核心。已通过 23 套件离线回归、25 个 PC 模拟器表单 GUI 步骤，确认最终 C4 的 27 张界面截图，完成 500 节点合成排序对照/原夹具恢复及平板 20 分钟导航观察。后续表单、持续观察及诊断脚本的纯模拟安全检查分别为 46/46、45/45、15/15，与历史套件记录分开归档。排序耗时包含 HDC/Inspector 开销，不代表网络测速或内存优化。

最终 C4 手机的 60 分钟导航观察本轮未完成，仅保留 65 个同进程、同候选包样本，实际跨度 1218.65 秒（约 20.31 分钟）；缺少完成摘要，中断原因未确认。先前 C3 约 37 分钟后失败的导航、已完成的 15 分钟空闲及 15 分钟界面检查对照均已保留，控制窗口平坦不能据此证明无泄漏。系统文件选择器仍受其触发的输入法协议阻断，未完成端到端保存/恢复；调试夹具恢复不能替代该流程。真实设备与 VPN 联网继续暂停，本地构建和原始记录不随源码发布，候选归属、历史失败和各套件计数详见[阶段十四记录](HarmonyVpnLab/docs/phase14-autonomous-refinement.md)。

## 0.13.0 — 2026-09-09

本次源码更新包含首次公开版本之后的 0.11–0.13 改进。应用版本为 `0.13.0/code34`。

### 节点管理与网络设置

- 增加域名和 IPv4 分流、局域网绕过选项，以及代理内 DNS over HTTPS 设置。
- 增加节点参数与单 outbound JSON 编辑、单节点导出、节点库备份及预览确认后恢复。
- 支持对筛选结果依次检测 HTTPS 请求耗时、取消后续检测，以及按耗时排序；保持当前选中节点不变。
- 修复节点菜单不展开、跨会话恢复计数干扰检测，以及上一检测进程退出前启动下一项的问题。

### 多端与日常操作

- 手机、平板与电脑共用按窗口宽度变化的布局；宽窗口采用侧栏和双栏首页，表单和节点列表限制最大宽度。
- 首页增加首次添加节点入口、网络设置摘要和快捷入口，重新安排宽屏中的连接、节点与流量信息。
- 节点列表突出当前节点，提供筛选数量、清空搜索及可展开的检测说明；宽屏使用紧凑信息行。
- 设置按用途分组，缩小外观选项，并修复大字号下三项外观选项宽高不齐的问题。
- 节点编辑增加未保存修改保护、固定保存反馈和端口校验；迟到的确认结果不会操作已离开的页面。
- PC 缺少系统扫码能力时，保留粘贴和 JSON/备份入口，按能力动态加载扫码模块。

### 构建与兼容

- 新增独立的 x86_64 模拟器界面预览构建，不包含 VPN 转发核心；正式 ARM64 构建保持独立。
- 最低兼容配置为 `6.1.1(24)`，编译和目标 SDK 保持 `26.0.0`。API 26 授权观察器增加版本/能力判断，启动超时与迟到结果按请求隔离。
- 设备测试支持显式指定 USB 目标，失联时不回落到其他设备，并适配大屏侧栏的滚动区域。

### 验证范围

ARM64 和 x86_64 调试签名构建通过。最终预览包在手机、平板和电脑模拟器上留存 16 张界面记录，包括窄窗口、大字号和深色模式；相关离线测试按套件记录在[阶段十三验证说明](HarmonyVpnLab/docs/phase13-product-refinement.md)。

0.11.1 的限定范围手机联网与备份恢复结果见[阶段十一记录](HarmonyVpnLab/docs/phase11-network-node-tools.md)。这些历史结果不替代 0.13.0 的联网验收：真实 API 24 平板的安装和联网仍暂停，真实鸿蒙电脑尚未联网验收；编辑确认弹窗、输入法和非法端口完整 GUI 流程仍待验证。此更新不包含通用发行 HAP，也不代表完成应用商店发布。
